#include<iostream>
#include<stack>
using namespace std;

struct Node {
    int data;
    struct Node *left, *right;
}
struct Node* newNode(int data) {

}
int main() {
    struct Node* root = newNode(1); 
}